# Test user accounts — copy to credentials.yaml and fill in real values
# credentials.yaml is gitignored and should never be committed
users:
  admin:
    email: admin@example.com
    password: changeme
    role: admin
  viewer:
    email: viewer@example.com
    password: changeme
    role: viewer
